#!/bin/bash -l
#SBATCH -N 1
#SBATCH -n 4
#SBATCH -o G2G_%A_%a.stdout
#SBATCH -e G2G_%A_%a.stderr
#SBATCH -p med

module load mummer


cd Chunk_${SLURM_ARRAY_TASK_ID}
touch ${SLURM_ARRAY_TASK_ID}.out
/home/dstorey/bin/Genome2GenomeDistance/G2GCalc --files *.fa* --threads 4  --out ${SLURM_ARRAY_TASK_ID}.out
cd ..
